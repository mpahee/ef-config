{
  "ConnectionStrings": {
    // SQL Server (Step 1/2) - kept for comparison against the Postgres format below.
    // "AppConnectionString": "Data Source=(localdb)\\MSSQLLocalDB;Initial Catalog=AppDataBaseInterview;"

    // Postgres/Npgsql (Step 3) - placeholder only. Real local credentials go
    // in appsettings.Local.json (gitignored) or environment variables /
    // GitHub Secrets in CI - never commit real credentials here.
    "AppConnectionString": "Host=localhost;Port=5432;Database=AppDataBaseInterview;Username=CHANGE_ME;Password=CHANGE_ME"
  }
}
