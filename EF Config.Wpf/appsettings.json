{
  "ConnectionStrings": {
    // Postgres/Npgsql - placeholder only. Real local credentials go in
    // appsettings.Local.json (gitignored) or environment variables /
    // GitHub Secrets in CI - never commit real credentials here.
    "AppConnectionString": "Host=localhost;Port=5432;Database=AppDataBaseInterview;Username=CHANGE_ME;Password=CHANGE_ME"
  }
}
